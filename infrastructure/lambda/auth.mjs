const COGNITO_ISSUER = `https://cognito-idp.${process.env.COGNITO_REGION}.amazonaws.com/${process.env.COGNITO_USER_POOL_ID}`;
const JWKS_URL = new URL(`${COGNITO_ISSUER}/.well-known/jwks.json`);
let jwksCache;
let jwksCacheExpiresAt = 0;
const log = (message, details = {}) => console.log(JSON.stringify({ scope: 'auth', message, ...details }));
const unauthorized = (message) => { const error = new Error(message); error.statusCode = 401; error.code = 'UNAUTHORIZED'; return error; };
const decodeBase64Url = (value) => Buffer.from(value.replace(/-/g, '+').replace(/_/g, '/'), 'base64');
const parseJwt = (token) => { const parts = token.split('.'); if (parts.length !== 3) throw unauthorized('Invalid bearer token'); try { return { header: JSON.parse(decodeBase64Url(parts[0]).toString('utf8')), payload: JSON.parse(decodeBase64Url(parts[1]).toString('utf8')), signature: decodeBase64Url(parts[2]), signingInput: Buffer.from(`${parts[0]}.${parts[1]}`) }; } catch { throw unauthorized('Invalid bearer token'); } };
const getJwks = async () => { if (jwksCache && Date.now() < jwksCacheExpiresAt) return jwksCache; log('loading Cognito JWKS', { url: JWKS_URL.toString() }); const response = await fetch(JWKS_URL); if (!response.ok) throw new Error(`Unable to load Cognito signing keys (${response.status})`); const body = await response.json(); if (!Array.isArray(body.keys)) throw new Error('Invalid Cognito signing keys'); jwksCache = body.keys; jwksCacheExpiresAt = Date.now() + 60 * 60 * 1000; log('Cognito JWKS loaded', { keyCount: body.keys.length }); return jwksCache; };
const verifySignature = async ({ header, signature, signingInput }) => { if (header.alg !== 'RS256' || !header.kid) throw unauthorized('Unsupported bearer token'); const key = (await getJwks()).find((candidate) => candidate.kid === header.kid); if (!key) throw unauthorized('Unknown bearer token key'); const cryptoKey = await crypto.subtle.importKey('jwk', key, { name: 'RSASSA-PKCS1-v1_5', hash: 'SHA-256' }, false, ['verify']); const valid = await crypto.subtle.verify('RSASSA-PKCS1-v1_5', cryptoKey, signature, signingInput); if (!valid) throw unauthorized('Invalid bearer token signature'); };
const getCognitoAttributes = async (accessToken) => { log('calling Cognito GetUser'); const response = await fetch(`https://cognito-idp.${process.env.COGNITO_REGION}.amazonaws.com/`, { method: 'POST', headers: { 'content-type': 'application/x-amz-json-1.1', 'x-amz-target': 'AWSCognitoIdentityProviderService.GetUser' }, body: JSON.stringify({ AccessToken: accessToken }) }); if (!response.ok) { const body = await response.text(); log('Cognito GetUser failed', { status: response.status, body: body.slice(0, 500) }); throw unauthorized('Unable to resolve Cognito user'); } const body = await response.json(); log('Cognito GetUser succeeded', { attributeNames: (body.UserAttributes ?? []).map((attribute) => attribute.Name) }); return Object.fromEntries((body.UserAttributes ?? []).map((attribute) => [attribute.Name, attribute.Value])); };
export const verifyBearerToken = async (authorizationHeader) => { log('starting bearer verification', { hasAuthorizationHeader: Boolean(authorizationHeader), authorizationPrefix: authorizationHeader?.slice(0, 7) }); if (!authorizationHeader?.startsWith('Bearer ')) throw unauthorized('Missing bearer token'); const token = authorizationHeader.slice('Bearer '.length).trim(); if (!token) throw unauthorized('Missing bearer token'); const parsed = parseJwt(token); log('token decoded', { sub: parsed.payload.sub, username: parsed.payload.username, tokenUse: parsed.payload.token_use, clientId: parsed.payload.client_id, issuer: parsed.payload.iss }); await verifySignature(parsed); const now = Math.floor(Date.now() / 1000); const { payload } = parsed; if (payload.iss !== COGNITO_ISSUER) throw unauthorized('Invalid token issuer'); if (payload.token_use !== 'access') throw unauthorized('Invalid token use'); if (payload.client_id !== process.env.COGNITO_APP_CLIENT_ID) throw unauthorized('Invalid token audience'); if (typeof payload.exp !== 'number' || payload.exp <= now) throw unauthorized('Expired bearer token'); const attributes = await getCognitoAttributes(token); const principal = { subject: payload.sub, username: payload.username, clientId: payload.client_id, claims: { ...payload, email: attributes.email, name: attributes.name, 'custom:role': attributes['custom:role'] } }; log('bearer verification succeeded', { subject: principal.subject, username: principal.username, role: principal.claims['custom:role'] }); return principal; };
